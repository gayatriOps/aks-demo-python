apiVersion: v1
kind: Service
metadata:
  name: aks-demo-python
  namespace: aks-demo-python
spec:
  type: LoadBalancer
  selector:
    app: aks-demo-python
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
